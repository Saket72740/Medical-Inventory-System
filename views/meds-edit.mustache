<!DOCTYPE html>
<head>
    <!-- CSS only -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <title>SK Medicine | Meds</title>
</head>
<body>
    <br/>
    <div class="container-sm">
        <h1>Edit the medicine that you selected</h1>
    </div>
    <br/>
    <div class="container-md">
        {{#med}}
        <form class="row" action='/meds/edit/{{id}}' method='post'>
            <div class="mb-3 row">
                <label class="col-sm-2 col-form-label">Medicine Name</label>
                <div class="col-sm-10">
                    <input type="text" name="name" value="{{name}}" placeholder="Name"/>
                </div>
            </div>
            <div class="mb-3 row">
                <label class="col-sm-2 col-form-label">Medicine Composition</label>
                <div class="col-sm-10">
                    <input type="text" name="composition" value="{{composition}}" placeholder="Composition"/>
                </div>
            </div>
            <div class="mb-3 row">
                <label class="col-sm-2 col-form-label">Medicine Brand</label>
                <div class="col-sm-10">
                    <input type="text" name="brand" value="{{brand}}" placeholder="Brand"/>
                </div>
            </div>
            <div class="mb-3 row">
                <label class="col-sm-2 col-form-label">Medicine Count</label>
                <div class="col-sm-10">
                    <input type="number" name="count" min="0" step="0" value="{{count}}" placeholder="Count"/>
                </div>
            </div>
            <div class="mb-3 row">
                <label class="col-sm-2 col-form-label">Medicine Price : <b>₹</b></label>
                <div class="col-sm-10">
                    <input type="number" name="price" min="0" step="0" value="{{price}}" placeholder="₹ Price"/>
                </div>
            </div>
            <div class="col-md-3 col-sm-3" style="margin:5px">
                <button type="submit" class="btn btn-primary mb-3">Save</button>
            </div>
        </form>
        {{/med}}    
    </div>
</body> 